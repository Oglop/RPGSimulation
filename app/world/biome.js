const { ENUM_BIOMS } = require('../constants')

const forest = {
    type: ENUM_BIOMS.forest
}

const grassPlains = {
    type: ENUM_BIOMS.grassPlains
}

module.exports = {
    forest,
    grassPlains
}
